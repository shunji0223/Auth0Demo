# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Auth0Demo' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'Auth0'
  pod 'Lock'
  pod 'JWTDecode'

  # Pods for Auth0Demo

  target 'Auth0DemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Auth0DemoUITests' do
    # Pods for testing
  end

end
